[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "noshow-iq-12826"
version = "0.1.0"
description = "A prediction API for patient no-shows by Ali 12826"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["noshow_iq*"]
